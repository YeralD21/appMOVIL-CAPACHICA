import 'package:flutter/material.dart';

class AsignarPermisosScreen extends StatelessWidget {
  const AsignarPermisosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Asignar Permisos'),
      ),
      body: const Center(
        child: Text(
          'Funcionalidad no disponible en esta versión.',
          style: TextStyle(fontSize: 18, color: Colors.grey),
        ),
      ),
    );
  }
} 